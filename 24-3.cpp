#include<math.h>
#include<iostream>
using namespace std;
int main()
{
    int x,y,c;
    cout<<"enter the number=";
    cin>>x>>y;
     c=pow(x,y);
    cout<<"the power of "<<x<<" is "<<y<<" is "<<c;
   return 0;
}